# AI 生成视频质量评测网站 — 产品需求文档（PRD）框架

> 版本：v0.2（任务一草稿，融入「个人自用/可分发/双击即开」约束，待确认后进入任务二/三）
> 适用范围：**个人自用 + 可分发**的本地 AI 视频质量评测工具（非 SaaS、非上线、无服务器）
> 评测基准对齐：VBench / VBench 2.0 / AIGVE-Bench / UVE-Bench（本平台取 10 个核心维度，可对标其子集）

---

## 0.0 交付与实现约束（v0.2 新增 — 核心架构前提）

本约束是后续所有模块设计的**硬前提**，违背即视为不合格：

1. **零基础设施**：不部署任何服务器、数据库服务、消息队列、对象存储。全部运行在用户本机。
2. **实现成本最小 / 项目最简洁**：单语言优先（Python 单栈），不引入微服务、前后端分离构建、容器。能用单文件/小文件夹解决的绝不分层。
3. **个人自用**：不设计多租户、鉴权体系、计费、并发扩容。专家/普通用户仅用本地「用户角色字段」区分，不建账户系统。
4. **可打包分发、双击即开**：最终产物是一个可拷贝到别人电脑、双击运行即打开浏览器界面的包；接收方无需装 Python/环境即可使用（或仅内置一键启动器）。
5. **客观评测可选化**：FVD 等重模型默认**不强制本地训练/下载**；自动打分以「用户填入自己的 LMM API Key 即插即用」为主，无 Key 时平台仅做轻量传统指标 + 人工 MOS，保证零成本可用。

> 由此，原 v0.1 中的 Celery/RQ 队列、GPU 推理池、S3 拉取、独立前端构建等设计**全部移除**，改为本地同步函数 + 单文件 Web UI。

---

## 0. 产品定位与竞品对标

### 0.1 产品一句话定位
一个**可双击运行的本地单文件 Web 应用**：融合「轻量客观指标 + 主观 MOS + 专家仲裁」三层评测，输出可与 VBench 对标、具备世界模型内在真实性细粒度诊断能力的评测报告；项目极简、零基础设施、可打包发给任何人即用。

### 0.2 竞品对标：相对 AIGVE-Tool 等开源框架的差异化（设计要点）

| 维度 | AIGVE-Tool / 常见开源评测 | 本平台设计改进点 |
|---|---|---|
| 评测层级 | 以「整体质量 + 语义对齐」为主，物理常识维度薄弱 | 显式引入**高层世界模型层**（物理规律/常识推理/人体结构/空间关系/可控性），作为独立评分子榜 |
| 评测范式 | 偏客观指标或单一主观打分 | **三范式融合**：客观(FVD/LMM) + 主观(MOS/A-B) + 专家仲裁，且分数可交叉验证 |
| 维度粒度 | 粗粒度（整体/分项） | **10 个核心维度**（可自定义加回精简维度），三层覆盖 |
| 一致性度量 | 通常不内置人工一致性分析 | 原生计算 **ICC 组内相关系数** 与 Krippendorff's α，量化标注员一致性 |
| 可对标性 | 自有格式，难以横向比较 | 输出 **VBench 兼容 schema**，支持导出 Leaderboard 格式 |
| 标注效率 | 播放+打分，缺乏校准 | 内置**反例校准库 + 技术失真/物理常识错误二元初判门控**，降低无效标注 |
| 可控性评测 | 多数框架缺失 | 显式「可控性」维度（姿态/轨迹/镜头语言可控度） |
| 部署形态 | 需命令行/环境配置，研究向 | **零基础设施本地应用**，打包后双击即开，可分发他人 |
| 成本门槛 | 常需 GPU / 多依赖 | 默认零成本（仅人工+轻量指标），LMM 自动打分按用户自带 API Key 即插即用 |

---

### 0.3 行业真实格局（2025–2026 市调）

> 经联网调研，AI 生成视频质量评测在 2025–2026 年**高度活跃**，但同行形态与我们差异显著。以下按三类划分，并对照本平台定位。

**A 类 · 学术 Benchmark / 开源评测框架（最密集，普遍评「模型」而非「单视频文件」）**

| 框架 | 机构/来源 | 形态与重点 |
|---|---|---|
| VBench 全系 | 南洋理工 S-Lab + 上海 AI Lab(OpenGVLab) + 港中文 + 南大 | VBench 1.0（16 维，CVPR'24 Highlight）、VBench++（TPAMI'25，扩 I2V/长视频/可信性）、VBench-2.0（2025-03，内在真实性：Human Fidelity/Controllability/Creativity/Physics/Commonsense，5 大类 18 细粒度能力维）。HuggingFace Leaderboard 持续更新（2026-08 仍有 HiDream-O1-Video、UNIPOW、Vidu Q1 等上榜） |
| WorldModelBench | UC Berkeley / UCSD / NVIDIA / MIT（2025） | 7 大领域、350 prompt、6.7 万人工标注；物理遵循占分一半；头部模型指令完成率仅 61%，12% 违反质量守恒 |
| VideoPhy / VideoPhy 2 | UC 系（2024/2025） | 物理常识评测（固-液/液-液交互） |
| Physics-IQ Benchmark | Google DeepMind（2025） | 物理推理评测 |
| PhyGenBench | OpenGVLab（2024） | 物理常识世界模拟评测 |
| IPV-Bench / Impossible Videos | Show Lab（2025） | 反物理/不可能视频评测 |
| AIGV-Assessor | CVPR'25 | 训练式 LMM 评估模型（AIGVQA-DB 36k 视频，4 维） |
| VideoScore | EMNLP'24 | 微调 Mantis 的 5 维评估模型 |
| Video-Bench | CVPR'25 | 全面用 MLLM + Chain-of-Query，与人类 Spearman 0.733 |
| EvalCrafter | CVPR'24 | 混合流水线（LLM-Judge + 传统视觉），代码未公开 |
| VQAScore / GenAI-Bench / t2v_metrics | CMU | 一行代码评测；被 DeepMind/字节 Seed/NVIDIA 采用，支持 GPT-4o/Gemini/Qwen3-VL |
| 专项 Bench | 多团队 | T2VBench、DEVIL、TC-Bench、GAIA、ADGV-Bench（自动驾驶）、GEOATTRACTION-500、AIGCBench（11 指标 4 维）、World Consistency Score(WCS，新兴统一指标) |

**B 类 · 在线榜单 / 半商业评测**
- VBench HuggingFace Leaderboard、LMArena Vision Arena（盲测 + Elo，Gemini 系列 2026 初包揽前六）
- 各生成平台（Sora / Veo / Runway / Kling / 即梦）内部自测，不对外提供工具

**C 类 · 与我们同形态（轻量 / 自用 / 单视频文件 / 即插即用）的同行**
- **基本空白。** 上述几乎都是「评模型」：喂固定 prompt 集批量生成、跑 Bench、上榜，面向研究者；普遍依赖特定 CV 模型或需训练的 LMM 奖励模型、需 GPU/学术环境，且不是开箱即用的个人工具。

**本平台差异化定位（与市调对照）**
1. 同行评「模型能力」；我们评「你手头任意一个 AI 视频文件」。
2. 同行需 GPU / 固定 prompt 集 / 学术环境；我们：上传即评、视觉 LMM 即插即用（用户自带 Key）、零基础设施、双击即开可分发。
3. 高层物理/常识/人体维（D08–D10）与 VBench-2.0 / WorldModelBench 方向高度同频，但用轻量 LMM 自动打分 + 人工 MOS + ICC 落地，而非重训练评测模型。
4. **结论**：轻量、自用、可分发、单视频级、视觉 LMM 即插即用的评测工具是市场空白，本工具定位成立且有价值。

---

## 1. 评测维度分层模型（总览 → 10 核心维度，v0.3 精简）

> **v0.3 精简原则**：原 18 维精简为 **10 个核心维度**，筛选标准是「最重要」且「均可由强视觉大模型（DeepSeek V4 视觉 / GPT-4o / Gemini / 本地 Ollama+DeepSeek-VL2 等）通过抽帧或整段观看直接打分」——即自动评测统一走 LMM 视觉判断，无需专用重模型，与「用户自备视觉 API Key」策略自洽。

平台评测维度遵循**三层金字塔模型**，映射到 10 个核心维度（人工标注台与 LMM 自动打分均以此为准）：

```
高层：世界模型与内在真实性 (World Model / Intrinsic Realism) — 3 维
  ├─ D08 物理规律（重力/碰撞/浮力/刚体运动）
  ├─ D09 人体动作与结构合理性（骨骼/关节/形变）
  └─ D10 常识推理（因果/物体功能/违背常识）

中层：内容语义对齐 (Semantic Alignment) — 3 维
  ├─ D05 主体一致性 (Subject Consistency)
  ├─ D06 背景一致性 (Background Consistency)
  └─ D07 文本-视频对齐 (Text-Video Alignment，需输入 prompt)

底层：基础技术质量 (Technical Quality) — 4 维
  ├─ D01 成像质量（噪点/模糊/压缩伪影）
  ├─ D02 美学质量（构图/色调/光影）
  ├─ D03 时序闪烁 (Temporal Flickering)
  └─ D04 运动平滑度 (Motion Smoothness)
```

> 说明：10 维兼顾三层，且全部为「视觉 LMM 可判」任务——底层靠看帧质量、中层靠跨帧比对主体/背景与读 prompt 对齐、高层靠视觉+常识推理。被精简掉的原维度（动态程度、整体一致性、多物体一致性、外观风格、空间关系、可控性、3D一致性、时序因果一致性）可作为「高级自定义维度」在配置表中按需加回，不进默认 10 维。
>
> 自动评测方式统一：对每维构造评分 Prompt，将视频（Gemini 整段 / 其他模型抽帧序列）送入视觉 LMM，输出 0–10 分 + 简短理由；无需 FVD/DOVER 等专用模型，零额外依赖。

---

## 2. 核心功能模块设计

### 模块 A｜评测任务管理（Evaluation Task Management）

**A.1 视频接入（本地优先，极简）**
- 支持本地上传（mp4/webm/mov，直接读取本机文件，不做强制转码——仅抽帧分析时用 OpenCV 按需解码）
- 支持可选 URL 拉取（仅公开直链，`requests` 下载到本地临时目录；**不接入 S3/对象存储**，无密钥体系）
- 轻量**文件指纹**：对视频算 MD5/帧哈希用于本机去重，避免同一文件重复建任务（本地 Set 即可，无需数据库）

**A.2 评测维度编排**
- 维度选择器以「三层金字塔」树形展示，支持：
  - 快捷模板：① 仅技术质量(4维) ② 技术+语义(7维) ③ 全量 10 维 ④ 仅世界模型层(3维)
  - 自定义组合：勾选任意 N 维 + 设定维度权重（默认等权）
- 评测模式选择：客观 / 主观 MOS / A-B 配对 / 混合

**A.3 任务编排与生命周期**
- 任务状态机：`Draft → Queued → Running(Auto) → Annotating(Human) → Arbitrating(Expert) → Completed`
- 每个任务生成唯一 `task_id`，关联视频、维度组合、模式、参与标注员列表

---

### 模块 B｜自动化评测流水线（本地同步调用，无队列/无 GPU 池）

**B.1 流水线阶段（本地函数串行调用）**
```
用户触发评测 → 抽帧(OpenCV, 按需解码)
  → 轻量客观指标(纯 CPU, 立即可用)
  → [可选] LMM 自动打分(用户填 API Key 后调用, AIGVQA 模式)
  → 分数归一化(0-10) → 写入本地 Scores(客观分)
```
> 无消息队列、无独立 worker。长任务（如 LMM 多轮打分）用 UI 后台线程 + 进度条，不阻塞主线程即可，不引入额外依赖。

**B.2 客观指标实现（默认零成本可用）**
- **轻量传统指标（纯本地、无需下载模型）**：
  - 时序闪烁 / 运动平滑度：相邻帧差分 + 光流（OpenCV）统计抖动/跳变
  - 成像质量（模糊/噪点）：拉普拉斯方差、BRISQUE（scikit-image 自带）
  - PSNR/SSIM：与参考帧或首帧比对（有参考时）
- **可选重指标（按需，非强制）**：`FVD`/`DOVER`/`LPIPS` 作为「插件式函数」，用户在设置里勾选并首次运行时自动 `pip` 拉取对应小模型权重；不勾选则平台照常工作
- **LMM 自动打分（AIGVQA 模式，即插即用）**：对 10 个核心维度逐一构造评分 Prompt，调用用户提供的视觉大模型 API（如 DeepSeek V4 视觉 / GPT-4o / Gemini / 本地 Ollama+DeepSeek-VL2），输出 0–10 分 + 简短理由。**Key 存于本机配置文件，不上传**

**B.3 调度与工程（极简）**
- 无 Celery/RQ/GPU 池。模型路由仅为 `if 维度 in 高层: 走 LMM else: 走信号指标` 的本地分支
- LMM 调用做温度采样去偏（每维 ≥2 次采样取均值），失败仅标记 `objective_unavailable`，不阻断流程
- 所有计算为本地函数，结果直接落本地存储（见模块 D 存储说明）

---

### 模块 C｜人工标注工作台（Human Annotation Workbench）

**C.1 前端交互布局**
- 左栏：视频播放器（支持**逐帧步进 / 0.25× 慢放 / A-B 同步双屏对比 / 时间轴打点**）
- 中栏：10 维度评分面板（按三层分组折叠，D01–D10）
- 右栏：当前维度锚定描述（Anchored Rating Scale）+ 反例参考缩略图

**C.2 评分机制**
- MOS 支持 5 分制或 10 分制（可在任务层配置）
- 每个维度采用**带锚定的滑块**：1 分/5 分/10 分对应明确行为描述（减少主观漂移）
- 记录标注员置信度（高/中/低）与「是否发现致命错误」标记

**C.3 配对偏好（A/B 测试）**
- 并排播放两视频，标注员仅选「更优 / 相当 / 更差」+ 优势维度标签
- 用于 Elo / Bradley-Terry 模型计算模型相对排名

**C.4 质量门控（关键，见任务三 SOP）**
- 提交前强制做**「技术失真 vs 物理常识错误」二元初判**，引导标注员区分两类失效，保证高层维度数据纯净

---

### 模块 D｜结果可视化面板（Result Visualization Dashboard）

**D.1 核心图表**
- **10 维雷达图**：单视频/单模型的能力轮廓，直观暴露短板层
- **分组柱状图**：各维度均值横向对比（多模型 / 同模型多版本）
- **MOS 分布直方图**：主观分分布 + 标注员间离散度
- **Leaderboard 表**：VBench 兼容格式，总分 + 分层小分排名

**D.2 一致性度量（学术严谨性）**
- 自动计算 **ICC（组内相关系数）** 与 **Krippendorff's α**，展示于面板，低于阈值的维度高亮预警
- 支持按标注员下钻，识别系统性偏差标注员

**D.3 对标与导出**
- 一键导出 **VBench / AIGVE-Bench 兼容 JSON**，支持与公开 Leaderboard 横向对标
- 报告含「世界模型能力子榜」独立视图（高层 D08–D10 三维）

---

## 2.5 本地存储方案（零数据库服务）

- **不使用 MySQL/Postgres 等数据库服务**。所有数据存于本机单一文件：
  - 主存储：`evaluation.db`（SQLite，单文件，随应用打包目录走）
  - 导出/交换：`*.json`（VBench 兼容 schema，便于发给他人或跨机汇总）
  - 视频与抽帧：`data/videos/`、`data/frames/` 本地目录
- 任务二将给出的 JSON Schema 既是「内存中的数据结构定义」，也直接对应 SQLite 表结构与 JSON 导出格式，三者统一，避免冗余映射。

---

## 5. 技术选型与本地打包方案（v0.2 新增）

### 5.1 推荐技术栈（单栈、极简）
| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11+（单栈） | 避免前后端双语言，CV/ML 生态齐全 |
| UI | **Gradio（已确认）** | 单文件即可渲染「播放器+评分面板+图表」，无需写 HTML/JS 构建链；天然 localhost Web，符合「网站」形态 |
| 视频处理 | OpenCV + imageio-ffmpeg | 抽帧/光流/基础指标，纯本地 |
| 图表 | Plotly（Gradio/Streamlit 内置或 `plotly`) | 雷达图/柱状图直接内嵌，无需 BI |
| 存储 | SQLite（标准库 `sqlite3`） | 单文件，零服务 |
| 统计 | `numpy` / `scipy` / `statsmodels` | ICC、Krippendorff's α 计算 |

> 不引入：React/Vue 前端工程、FastAPI+独立前端、Celery、Docker、任何云服务。
> **LMM 自动打分（已确认策略）**：用户自备具备视觉能力的 API Key（支持 GPT-4o / Gemini 原生视频 / 本地 Ollama 加载 DeepSeek-VL2 等），Key 仅存本机。注意：DeepSeek 官方文本 API(V3/R1) 无视觉能力，须走 VL2+Ollama 抽帧方案；视频评测对象为「AI 生成的视频」本身，非视频生成模型。

### 5.2 打包与分发（双击即开）
- **方案 A（推荐，最简）**：用 **PyInstaller** 将 Gradio/Streamlit 应用打包为单目录 / 单文件可执行（Windows `.exe` / macOS `.app`）。接收方双击 → 自动起 localhost 服务并打开浏览器，无需装 Python。
- **方案 B（更轻量包体）**：用 **Tauri** 包裹同一套静态前端 + 内嵌最小 Python（或纯 JS 实现指标），产物仅数 MB~数十 MB；但需把 Python 逻辑改写为可在 WebView 内运行（成本略高，非首选）。
- **方案 C（零打包，给技术用户）**：直接发源码文件夹 + 一键 `run.bat`（`python -m pip install -r requirements.txt && python app.py`），接收方本机有 Python 即可。
- **分发形态**：一个压缩包（含 exe + 示例数据 + 说明），拷贝到任意电脑解压双击即用；评测结果以 JSON/SQLite 文件通过微信/邮件回传汇总。

---

## 3. 如何在架构中融入「内在真实性（物理与常识）」评测维度

这是本平台相对开源框架的核心增量，落地方式如下：

1. **Schema 层标记**：所有维度配置带 `layer` 字段（technical / semantic / world_model）。高层维度在数据模型中独立成列，不与传统质量分混算，单独构成「世界模型能力子榜」。
2. **自动评测专用路由**：物理/常识维度调用强推理 LMM + 结构化 CoT 提示（如「判断视频中物体是否违反重力/碰撞/浮力，给出违反类型与时间戳」），而非依赖浅层视觉指标。
3. **人工标注隔离页签**：高层 3 维（D08–D10）在标注台有独立 Tab，并配**反例校准库**（标准物理/常识违规样例缩略图），标注前强制校准，降低误判。
4. **专家仲裁兜底**：高层维度默认开启「专家陪审团仲裁」权限——标注员提交后，专家可对争议样本做最终裁定，仲裁分覆盖普通标注均值。
5. **诊断而非仅打分**：高层维度输出不仅给分，还结构化记录「违规类型 + 时间戳 + 置信度」，使报告具备可解释的诊断价值（优于开源框架的单一分数）。

---

## 4. 决策状态（进入任务二/三前）

> **已确认（第 1 轮）**：
> - UI 框架 = **Gradio**
> - 打包方案 = **PyInstaller 单 exe（默认 Windows）**
> - MOS 分制 = **10 分制**
> - LMM 自动打分 = **用户自备具备视觉能力的 API Key**（DeepSeek V4 视觉 / GPT-4o / Gemini / 本地 Ollama+DeepSeek-VL2），评测对象为「AI 生成的视频」本身
>
> **已确认（第 2 轮，v0.3 精简）**：
> - 评测维度 = **从 18 维精简为 10 个核心维度**（底层4/中层3/高层3），全部可由强视觉 LMM 直接打分；被精简维度留作「高级自定义维度」可加回
> - 可控性 = **不细分**
> - 竞品对标 = **不补充**（维持 AIGVE-Tool / VBench / AIGVE-Bench / UVE-Bench）

### 4.1 全部决策已锁定 → 进入任务二/三
任务一 PRD 框架（v0.3）已完整确认，可进入：
- **任务二：数据库 Schema（JSON 示例，对应 SQLite 表 + VBench 兼容导出）**
- **任务三：评测员 SOP 操作规范**

> 注：自动评测统一走视觉 LMM 对 10 维打分，无 FVD/DOVER 等专用模型依赖，与「零成本/最小实现」约束一致。
